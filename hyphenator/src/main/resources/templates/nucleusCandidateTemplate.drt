template header
symbol

template "Letter is a nucleus candidate"

rule "@{symbol} is a nucleus candidate"
when
    $c : model.Letter(symbol == '@{symbol}')
then
    $c.setRole(model.LetterRole.NUCLEUS_CANDIDATE);
    update($c);
end
end template