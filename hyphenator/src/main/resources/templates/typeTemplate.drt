template header
symbol
type

template "Classify letter as type"

rule "Classify @{symbol} as @{type}"
when
    $c : model.Letter(symbol == "@{symbol}")
then
    $c.getTraits().setType(model.PhonemeType.@{type});
    update($c);
end
end template