template header
symbol
sonority

template "Determine letter phonetic traits"

rule "Measure @{symbol} sonority as @{sonority}"
salience 100
when
    $c : com.ftn.sbnz.model.models.Letter(symbol == "@{symbol}")
then
    $c.getTraits().setSonority(@{sonority});
    update($c);
end
end template