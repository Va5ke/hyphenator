template header
symbol
type

template "Classify letter as type"

rule "Classify @{symbol} as @{type}"
salience 100
when
    $c : com.ftn.sbnz.model.models.Letter(symbol == "@{symbol}")
then
    $c.getTraits().setType(com.ftn.sbnz.model.models.PhonemeType.@{type});
    update($c);
end
end template