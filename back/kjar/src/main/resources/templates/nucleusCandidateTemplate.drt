template header
symbol

template "Letter is a nucleus candidate"

rule "@{symbol} is a nucleus candidate"
when
    $c : com.ftn.sbnz.model.models.Letter(symbol == '@{symbol}')
then
    $c.setRole(com.ftn.sbnz.model.models.LetterRole.NUCLEUS_CANDIDATE);
    update($c);
end
end template